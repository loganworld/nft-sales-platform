import {
  Card
} from "@mui/material"
import { styled } from '@mui/material/styles';

export const ItemCard = styled(Card)(({
  maxWidth: "250px",
  margin: "auto",
  paddingTop: "16px",
  paddingInline: "16px"
}))